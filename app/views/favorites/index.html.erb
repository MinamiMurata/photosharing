<h2>お気に入り一覧</h2>

<div class="row mb-5">
  <% if @favorites.present? %>
    <% @favorites.each do |favorite| %>
      <div class="col-md-4">
        <div class="card mb-4 shadow-sm card-body">
          <p class="card-text">
            <% if favorite.picture.user.profile_picture.present? %>
              <%= image_tag(favorite.picture.user.profile_picture.url, height: '30') %>
            <% else %>
            <% end %>
          <%= link_to favorite.picture.user.name, user_path(favorite.picture.user.id)%>
          </p>
          <p class="bd-placeholder-img card-img-top"><%= image_tag(favorite.picture.image.url, width: "100%") %></p>
          <div class="card-body">
            <p class="card-text"><%= favorite.picture.content %></p>
            <div class="d-flex justify-content-between align-items-center">
              <div class="btn-group">
                <%= link_to '確認', picture_path(favorite.picture.id), type: "button", class: "btn btn-outline-secondary" %>
              </div>
            </div>
          </div>
        <small class="text-muted"><%= time_ago_in_words(favorite.picture.created_at) %></small>
        </div>
      </div>
    <% end %>
  <% else %>
    <p>登録しているお気に入りはありません。</p>
  <% end %>
</div>

<%= link_to '戻る', user_path(current_user), class: "mt-3 btn btn-outline-danger btn-lg" %>
<%= link_to 'Photos', pictures_path, class: "mt-3 ms-3 btn btn-outline-danger btn-lg" %>